let A = prompt("Digite o numero para A: "); //declarar A para receber o valor com o prompt.
let B = prompt("Digite o numero para B: "); //declarar B para receber o valor com o prompt.

A = parseInt(A); // converte string à inteiro.
B = parseInt(B); // converte string à inteiro.

document.write("X = "+(A+B)); //imprime na tela o resultado.